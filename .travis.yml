language: java

jdk:
  - oraclejdk8

os:
  - linux

before_script:
  - echo "MAVEN_OPTS='-Xmx2g'" > ~/.mavenrc
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script:
- mvn clean install